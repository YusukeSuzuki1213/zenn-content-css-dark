{
  "name": "zenn-content-css-dark",
  "version": "0.2.0",
  "license": "MIT",
  "description": "Zenn flavor content style with a dark theme.",
  "main": "lib/index.css",
  "files": [
    "lib/index.css"
  ],
  "scripts": {
    "dev": "sass ./src/index.scss ./lib/index.css --watch --no-source-map",
    "build": "sass ./src/index.scss ./lib/index.css --style compressed --no-source-map",
    "test": "echo 'no test yet'",
    "lint": "stylelint \"**/*.scss\""
  },
  "devDependencies": {
    "sass": "^1.58.1",
    "stylelint": "^16.2.1",
    "stylelint-config-recommended-scss": "^14.0.0"
  }
}
